#!/usr/bin/perl
#array.cgi - uses the pop, shift, and splice functions
use strict;

#declare variables
my @names = ("Beth", "John", "Suman", "Paula", "Carl");

#display original @names array
print 'Original @names array: ';
foreach my $x (@names) {
	print "$x ";
}
print "\n";


#remove last element from @names array

#remove first element from @names array

#remove one or more elements from the @names array

#replace one or more elements in the @names array

#insert one or more new elements in the @names array

#display modified @names array
print 'Modified @names array: ';
foreach my $x (@names) {
	print "$x ";
}
print "\n";
